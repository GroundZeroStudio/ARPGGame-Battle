using INFramework.UI;
using System.Collections;
using System.Collections.Generic;
using UI;
using UnityEngine;

namespace Game
{
	public class GameLauch : MonoBehaviour
	{
        private void Start()
        {
            //UIManager.Instance.Init();
            //UIManager.Instance.ShowPanel<TestPanel>();
            //UIManager.Instance.Init(UIRoot.Instance.UI_Root.transform as RectTransform, UIRoot.Instance.PanelRoot as RectTransform, UIRoot.Instance.UICamera, UIRoot.Instance.EventSystem);
            //UIManager.Instance.Register<>
        }
    }
}

